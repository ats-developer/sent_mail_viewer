<% flash.each do |type, message| %>
    <div class="alert <%= alert_class_for(type) %> fade in">
      <button class="close" data-dismiss="alert" onClick="$('.alert').fadeOut();" style="border: none; background: transparent;">×</button>
      <%= message %>
    </div>
<% end %>